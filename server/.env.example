# === CORS ===
# Comma-separated list OR "*" to allow all.
# Ex.: http://127.0.0.1:5500,https://yourdomain.com
ALLOWED_ORIGINS=http://127.0.0.1:5500,*

# === Generic LLM config (preferred) ===
# If unset, the app falls back to GROQ_* below.
# Groq (OpenAI-compatible):
#   LLM_API_URL=https://api.groq.com/openai/v1/chat/completions
#   LLM_MODEL=llama-3.1-8b-instant
# Gemini:
#   LLM_API_URL=https://generativelanguage.googleapis.com/v1beta
#   LLM_MODEL=gemini-1.5-flash
LLM_API_URL=
LLM_API_KEY=
LLM_MODEL=

# === Back-compat (fallbacks) ===
# Used if LLM_API_KEY/URL/MODEL are not provided.
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# === GitHub authentication (choose one) ===
# 1) Personal Access Token (simple):
GITHUB_PAT=

# 2) GitHub App:
GITHUB_APP_ID=
GITHUB_INSTALLATION_ID=
# Option A: load PEM from disk (used by _via_app when path exists)
GITHUB_PRIVATE_KEY_PATH=./keys/github_app.pem
# Option B: provide PEM base64 (used when *_pem_b64 is present in request body)
# Leave empty here; typically passed per-request.
GITHUB_PRIVATE_KEY_PEM_B64=
